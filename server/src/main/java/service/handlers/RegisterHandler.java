package service.handlers;

//this class takes in a JSON object, converts it to a Register Request object
//then the run method will make a service request with that Register Request and
//conver the return value back into a JSON object to send back to the Server
public class RegisterHandler {
    //RegisterRequest;
    public RegisterHandler(){

    }
}
